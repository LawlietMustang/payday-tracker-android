# WageTrack 2.4.3

Replaces the old clock/euro logo with the supplied WageTrack rising-bar SVG in onboarding, the menu and Android launcher. The adaptive icon preserves the exported vector paths and clip, with padding for launcher masks. Application ID, signing certificate and saved data format are unchanged.
